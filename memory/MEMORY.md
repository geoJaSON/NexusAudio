# Memory Index

- [NEXUS//AUDIO locked decisions](nexus-audio-locked-decisions.md) — SQLite@50k, symphonia+cpal pending spike, audiobook resume is the acceptance test, no speed control, tag editing deferred to Phase 8

## Reminders

- **Create a `.deb` package** for Linux install (user has `target/release/nexus-audio` built; Windows has `NexusAudio.iss`). Track work in `packaging/debian/TODO.md`.
